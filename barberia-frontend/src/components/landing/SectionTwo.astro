---
import { Image } from "astro:assets";
import DashboardLight from "@/images/dashboardLight.svg";
import Text from "@/components/fundations/elements/Text.astro";
import Button from "@/components/fundations/elements/Button.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
import Check from "@/components/fundations/icons/Check.astro";

const sections = [
  {
    title: "Interface Sencilla",
    description:
      "Panel diseñado para usarse desde el día uno, sin necesidad de capacitación técnica.",
    features: ["Vista de agenda diaria y semanal", "Acceso completo desde celular"],
  },
  {
    title: "Analítica en Tiempo Real",
    description:
      "Entiende el rendimiento de tu negocio con reportes actualizados al instante.",
    features: ["Reportes de ventas por período", "Comisiones desglosadas por barbero"],
  },
  {
    title: "Automatizaciones",
    description:
      "Reduce el trabajo administrativo y deja que el sistema trabaje por ti.",
    features: ["Recordatorios de citas automáticos", "Alertas de inventario bajo"],
  },
];
---

<section class="bg-base-950">
  <Wrapper variant="standard" class="py-20">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-12 items-center">
      <div class="lg:order-last">
        <Text
          tag="span"
          variant="textSM"
          class="leading-normal font-medium uppercase tracking-widest text-accent-500"
        >
          Más beneficios
        </Text>
        <Text
          tag="h2"
          variant="displayMD"
          class="tracking-tight mt-4 font-semibold text-base-50 font-display"
        >
          Diseñado para el día a día de la barbería
        </Text>
        <Text tag="p" variant="textBase" class="mt-4 text-base-400 font-medium">
          Simplifica la operación y enfócate en lo que mejor sabes hacer.
        </Text>
        <div class="flex flex-wrap items-center gap-2 mx-auto mt-12">
          <Button
            variant="default"
            size="sm"
            isLink
            href="/signup">Crear Cuenta</Button
          >
          <Button
            variant="muted"
            size="sm"
            isLink
            href="#features"
          >
            Ver Funciones</Button
          >
        </div>
      </div>
      <div class="lg:col-span-2">
        <Image
          alt="Panel de gestión de barbería"
          width={900}
          height={700}
          src={DashboardLight}
          class="object-cover h-full ring-1 ring-accent-800/50 border border-base-800 rounded-2xl opacity-90"
        />
      </div>
    </div>
    <div
      class="gap-x-2 gap-y-14 mt-12 lg:gap-12 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"
    >
      {
        sections.map((section) => (
          <div class="p-6 rounded-xl bg-base-900 border border-base-800">
            <div>
              <Text tag="h3" variant="textLG" class="text-base-100 font-medium font-display">
                {section.title}
              </Text>
              <Text tag="p" variant="textBase" class="mt-2 text-base-400">
                {section.description}
              </Text>
            </div>
            <ul
              role="list"
              class="space-y-1 mt-6 text-base font-medium text-base-100"
            >
              {section.features.map((feature) => (
                <li>
                  <div class="flex items-center gap-4">
                    <Check class="size-5 text-accent-500 shrink-0" />
                    <Text tag="p" variant="textSM" class="text-base-400">
                      {feature}
                    </Text>
                  </div>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>
  </Wrapper>
</section>
